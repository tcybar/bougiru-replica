{
  "files": [],
  "references": [
    {
      "path": "./tsconfig.app.json"
    },
    {
      "path": "./tsconfig.node.json"
    }
  ],

  // TODO: tsconfig.app.jsonにも同じ記載があるので、どうにかしたい
  "compilerOptions": {
    /* import alias */
    "baseUrl": "./",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
